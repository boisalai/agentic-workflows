[project]
name = "agentic-workflows"
version = "0.1.0"
description = "Learning agentic workflows with LangGraph and Ollama"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain>=0.3.0",
    "langchain-community>=0.3.0",
    "langgraph>=0.2.0",
    "ollama>=0.3.0",
]
